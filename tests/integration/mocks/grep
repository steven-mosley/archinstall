#!/bin/bash
# Mock grep command

# If looking for RAM info
if [[ "$1" == "MemTotal" && "$2" == "/proc/meminfo" ]]; then
    echo "MemTotal:       16384000 kB"
    exit 0
fi

# Pass through to real grep for other cases
command grep "$@"
